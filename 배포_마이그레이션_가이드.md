# 현장관리 앱 — NAS 탈출 마이그레이션 가이드

이 문서는 시놀로지 NAS에서 벗어나 **Vercel(앱) + Neon(DB) + Cloudflare R2(사진)** 조합으로
옮기기 위해 사용자님이 직접 해야 하는 계정 생성·설정 단계를 정리한 것입니다.

계정 생성과 비밀번호/API 키 입력은 보안상 AI가 대신 할 수 없는 영역이라, 아래 단계는
직접 진행해 주셔야 합니다. 각 단계 사이에 막히는 부분이 있으면 캡처해서 보여주시면
이어서 도와드리겠습니다.

전부 무료이며, 셋 다 "몇 주씩 접속이 없어도 죽지 않는" 조건을 만족합니다.

---

## 0. 준비물

- GitHub 계정 (이미 `slkamg0-ai/Building-Field-Management` 저장소가 있어 그대로 사용)
- 이메일 주소 하나 (Neon, Vercel, Cloudflare 가입용 — 기존 Google/GitHub 계정으로 소셜 로그인 가능)

---

## 1. Neon — 데이터베이스

1. https://neon.tech 접속 → **Sign up** → GitHub 계정으로 로그인 (가장 간단)
2. 프로젝트 생성 화면에서:
   - Project name: `field-management` 등 원하는 이름
   - Region: **AWS Asia Pacific (Seoul)** 선택 (한국에서 접속 속도가 가장 빠름)
   - Postgres 버전: 기본값 그대로 두면 됩니다
3. 프로젝트가 생성되면 대시보드에 **Connection string**이 두 종류 보입니다.
   - "Pooled connection" → 이 값을 `.env`의 `DATABASE_URL`에 붙여넣기
   - "Direct connection" (또는 Pooled 옆 토글을 꺼서 나오는 버전) → `DIRECT_URL`에 붙여넣기
   - 둘 다 `postgresql://...`로 시작하고 끝에 `?sslmode=require`가 붙어 있어야 합니다.
4. **기존 Supabase 데이터 이전이 필요하면** 알려주세요 — 데이터가 있는 상태면 마이그레이션 스크립트를 별도로 준비해드리겠습니다. 아직 데이터가 거의 없다면 그냥 새로 시작하는 게 가장 깔끔합니다.

---

## 2. Cloudflare R2 — 사진/이미지 저장

1. https://dash.cloudflare.com 접속 → 계정 생성 (또는 로그인)
2. 왼쪽 메뉴에서 **R2 Object Storage** 클릭 → 처음이면 결제수단 등록을 요구할 수 있는데, **무료 한도(10GB) 안에서는 과금되지 않습니다.** (초과 시에만 자동 과금되므로, 걱정되면 등록 후 Billing에서 사용량 알림을 설정해두세요.)
3. **Create bucket** 클릭:
   - Bucket name: `field-management-photos`
   - Location: Automatic
4. 버킷 생성 후 **Settings** 탭 → **Public access** → **Allow Access**를 켜서 `r2.dev` 공개 URL을 활성화합니다. 화면에 나오는 `https://pub-xxxxxxxx.r2.dev` 주소를 복사해 `.env`의 `R2_PUBLIC_URL`에 붙여넣습니다.
5. R2 메인 화면 우측 상단 **Manage R2 API Tokens** → **Create API Token**:
   - Permissions: **Object Read & Write**
   - 특정 버킷(`field-management-photos`)에만 권한을 제한하는 걸 추천
   - 생성하면 **Access Key ID**와 **Secret Access Key**가 딱 한 번 보여집니다 — 바로 복사해서 `.env`의 `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`에 저장하세요.
6. Cloudflare 대시보드 우측 상단 계정 개요에서 **Account ID**(영숫자 32자리)를 확인해 `.env`의 `R2_ACCOUNT_ID`에 붙여넣습니다.
7. `R2_BUCKET_NAME=field-management-photos`

---

## 3. Vercel — 앱 배포

1. https://vercel.com 접속 → **Sign up** → GitHub 계정으로 로그인
2. **Add New → Project** → GitHub 저장소 목록에서 `Building-Field-Management` 선택 → **Import**
3. Framework Preset은 Next.js가 자동 감지됩니다. 건드릴 필요 없습니다.
4. **Environment Variables** 섹션에서 `.env` 파일에 있는 항목을 전부 하나씩 입력합니다 (Neon 2개, R2 5개, 기존 Google Gemini API 키, VAPID 키 등 전부).
5. **Deploy** 클릭. 2~3분 내로 첫 배포가 끝나고 `https://프로젝트명.vercel.app` 주소가 생깁니다.
6. 배포 후 최초 1회, DB에 테이블을 만들어야 합니다 — 로컬 컴퓨터에서 다음을 실행:
   ```bash
   # .env에 위에서 받은 Neon DATABASE_URL / DIRECT_URL이 들어있는 상태에서
   npx prisma migrate deploy
   ```
   (이 명령은 Vercel이 아니라 사용자님 컴퓨터에서, 방금 만든 Neon DB를 향해 실행하는 겁니다.)
7. 이후로는 `git push`만 하면 Vercel이 자동으로 재배포합니다.

---

## 4. 기존 사진 이전 (선택, 나중에 해도 됨)

지금 NAS `/uploads` 폴더에 저장된 기존 사진들은 자동으로 R2에 옮겨지지 않습니다. NAS가 아직 켜져 있는 동안 폴더 접근 방법을 알려주시면, 일괄 업로드 스크립트를 만들어 드리겠습니다. 급하지 않으면 이 단계는 나중에 진행하고, 우선 새로 찍는 사진부터 R2로 정상 저장되는지 먼저 확인하는 걸 추천합니다.

---

## 5. 체크리스트

- [ ] Neon 프로젝트 생성, DATABASE_URL / DIRECT_URL 확보
- [ ] R2 버킷 생성, 공개 URL 활성화, API 토큰 발급
- [ ] `.env` 파일에 위 값 전부 채우고 로컬에서 `npm install && npm run dev`로 정상 동작 확인
- [ ] `npx prisma migrate deploy`로 Neon에 테이블 생성
- [ ] Vercel 프로젝트 생성, 환경변수 입력, 배포
- [ ] 배포된 주소에서 로그인 → 사진 업로드 테스트 (R2에 정상 저장되는지)
- [ ] 직원 3명에게 새 주소 공유
- [ ] (선택) 기존 NAS 사진 이전
- [ ] NAS 웹서버(Docker 컨테이너) 종료

막히는 단계가 생기면 화면 캡처와 함께 말씀해 주세요.
