version: '3.8'

services:
  # ── PostgreSQL DB ────────────────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: field-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: fieldmanagement
      POSTGRES_USER: fielduser
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fielduser -d fieldmanagement"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── Field manage 앱 (신규 독자 도커 서비스) ─────────────
  field-manage:
    build:
      context: .
      dockerfile: Dockerfile
    image: prince211/field-manage:latest
    container_name: field-manage-app
    restart: unless-stopped
    ports:
      - "3200:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://fielduser:${POSTGRES_PASSWORD}@postgres:5432/fieldmanagement
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./uploads:/app/uploads

volumes:
  postgres_data:

networks:
  default:
    name: nas-services
    external: false
